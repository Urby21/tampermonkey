# Release folder

`dist/` je pracovni adresar pro hotove kopie skriptu k vlozeni do Tampermonkey nebo Chrome snippets.

Zdroj pravdy je porad `src/`. Kopie do `dist/` pripravis prikazem:

```sh
sh scripts/release.sh
```
